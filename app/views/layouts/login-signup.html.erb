<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Steak And Eggs" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_pack_tag "application" %>
  </head>

  <body>
	  
    <%= yield %>
  </body>
</html>
